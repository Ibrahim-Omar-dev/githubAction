on: 
  workflow_dispatch:
    inputs:
      name:
          description: "Hema"
          required: true
jobs:
  test:
     runs-on: ubuntu-latest
     steps:
       - run: echo Hello ${{ github.event.inputs.name }}
