on: 
  workflow_dispatch:
jobs:
  test:
     runs-on: ubuntu-latest
     steps:
       - name: set variables
         run: echo "NAME=Ibrahim">> $GITHUB_ENV
       - name: print variables
         run: echo $NAME
